# cursor-2026-08-19-bridge-usage

## Goal

Document path + passphrase triggers for the Wisp–Cursor bridge, with a
bilingual usage page in this adapter repo.

## Done

- Added `docs/bridge-usage.html` (中 / EN toggle, `localStorage` key
  `wisp-bridge-lang`).
- README points at that page.
- Generic adapter HANDOFF (no topic-folder names).
- Catalog row for `/ws-continue`.
- Project copies of `ws-continue` and `wisp-bridge.mdc` stay in this repo.

## Open

Test the trigger in a topic folder (not this adapter) after Reload Window.
